[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "goat-ts-core"
version = "1.0.0"
description = "Rust core for GOAT-TS CIG (graph + wave propagation)"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
